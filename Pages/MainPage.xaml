<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:IPM_Proj"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:Microsoft.Toolkit.Uwp.UI.Controls"
    x:Class="IPM_Proj.MainPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition>

            </ColumnDefinition>
            <ColumnDefinition>

            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Margin="10,10,10,10">
            <Custom:DataGrid x:Name="currencyGridUWP"  HorizontalAlignment="Center" Margin="0,0,0,0" VerticalAlignment="Center" Height="782" Width="562" ItemsSource="{x:Bind CurrencyCollection}"/>
        </Grid>
        <ListView x:Name="lastDatesListViewUWP" Grid.Column="1" HorizontalAlignment="Left" Height="746" Margin="192,221,0,0" VerticalAlignment="Top" Width="440" ItemsSource="{x:Bind LatestDatesCollection}"/>

    </Grid>
</Page>
